# Protect database folder from direct access
# SQL files and database scripts should never be accessible

# Deny all direct access
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
