# Protect robot-engine folder from direct access
# Node.js files should not be served by Apache

# Deny all direct access
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
